# Backend middleware — calls the Research API over HTTP.
# Heavy data/ML deps (yfinance, transformers, torch, ollama) live in the RESEARCH repo,
# not here. This service only needs the web stack, DB, auth, HTTP client, and Claude SDK.
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# Database + auth
sqlalchemy==2.0.35
bcrypt==4.2.0
pyjwt==2.9.0
email-validator==2.2.0

# HTTP client to the Research API
httpx==0.27.2

# Agentic chat (tool-use loop)
anthropic==0.34.0
pytest-cov==5.0.0
